export default function App() {
  return (
    <main className="foundation-shell">
      <section aria-labelledby="foundation-title">
        <p className="eyebrow">Admin Portal</p>
        <h1 id="foundation-title">MediCore — Cổng quản trị</h1>
        <p>Cấu hình danh mục, RBAC, audit và hệ thống.</p>
        <p className="eyebrow">Development baseline — no workflow active yet.</p>
      </section>
    </main>
  )
}
