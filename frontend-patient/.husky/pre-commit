#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🔍 Running pre-commit checks: TypeScript typecheck & ESLint..."

npm run typecheck || exit 1
npm run lint || exit 1

echo "✅ Pre-commit verification passed successfully!"
